# Local MCP Server Configuration Template
# Copy this file to .env and configure for your environment

#====================================#
# Project Paths
#====================================#

# Main project path (for mounting in Docker, default to current directory)
PROJECT_PATH=/path/to/your/project

# Allowed directories for file operations (comma-separated)
ALLOWED_DIRECTORIES=/path/to/allowed,/another/path

# Git repository path (defaults to PROJECT_PATH)
GIT_REPO_PATH=/path/to/git/repo

#====================================#
# GitHub Integration (Optional)
#====================================#

# GitHub personal access token
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, read:org (adjust based on your needs)
GITHUB_TOKEN=ghp_your_token_here

#====================================#
# VS Code Settings (Optional)
#====================================#

# URL for headless VS Code service
VSCODE_SERVICE_URL=http://localhost:5007

# VS Code web interface port
VSCODE_PORT=3000

# Diagnostics service API port
DIAGNOSTICS_PORT=5007

#====================================#
# Security Settings
#====================================#

# Disable terminal features (true/false)
DISABLE_TERMINAL=false

# Disable debug features (true/false)
DISABLE_DEBUG=false

# Enable security logging (true/false)
SECURITY_LOGGING=true

# Comma-separated list of allowed VS Code commands (empty = allow all)
# ALLOWED_VSCODE_COMMANDS=workbench.action.files.save,editor.action.formatDocument

# Comma-separated list of allowed extensions (empty = allow all)
# ALLOWED_EXTENSIONS=ms-python.python,dbaeumer.vscode-eslint
