# Environment Configuration for MCP VSCode Project

# ============================================
# Repo MCP Server Configuration
# ============================================

# Comma-separated list of directories the server is allowed to access
ALLOWED_DIRECTORIES=/path/to/your/projects

# ============================================
# VSCode MCP Server Configuration
# ============================================

# URL of the VS Code headless service running inside Docker
VSCODE_SERVICE_URL=http://localhost:5007

# Request timeout in milliseconds
VSCODE_SERVICE_TIMEOUT=30000

# ============================================
# Docker Configuration
# ============================================

# Path to the project you want to mount in the container
PROJECT_PATH=/path/to/your/project

# Port for the VS Code web interface
VSCODE_PORT=3000

# Port for the VS Code service API
DIAGNOSTICS_PORT=5007

# Optional: Authentication token for VS Code server
# VSCODE_AUTH_TOKEN=your-secure-token

# ============================================
# Safety Configuration
# ============================================

# Disable terminal functionality (for security in shared environments)
DISABLE_TERMINAL=false

# Disable debug functionality
DISABLE_DEBUG=false

# Enable verbose logging for sensitive operations
SECURITY_LOGGING=true

# ============================================
# Allowlists (leave empty to allow all)
# ============================================

# Comma-separated list of allowed VS Code command IDs
# Example: workbench.action.files.save,editor.action.formatDocument
ALLOWED_VSCODE_COMMANDS=

# Comma-separated list of allowed extension IDs
# Example: ms-python.python,dbaeumer.vscode-eslint
ALLOWED_EXTENSIONS=
